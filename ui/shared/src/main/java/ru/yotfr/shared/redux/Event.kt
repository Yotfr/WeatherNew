package ru.yotfr.shared.redux

/**
 * While this interface is empty, it is used to clearly defined ant class that is representative
 * of an Event for a given screen, that shouldn't be stored in the State
 */
interface Event